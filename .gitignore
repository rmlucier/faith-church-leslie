# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Vercel
.vercel

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env — PCO secrets never get committed
.env
.env.*
!.env.example
# .env.production holds ONLY the public VITE_SITE_URL (no secrets — VITE_ vars
# are bundled into the client anyway). Committed so Vercel substitutes it at build.
!.env.production
